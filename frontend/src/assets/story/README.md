# Story PNG placeholders

Overwrite these files with your own art. Keep the same filenames so stories
keep working. Keys are referenced from `backend/src/content/modules.config.js`
(e.g. `apple` → `apple.png`, background `market` → `market.png`).
