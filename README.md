# Vizma

A tool that helps parents teach their neurodivergent child math by turning
lesson PDFs into short, visual, story-based mini-lessons.

**For setup instructions, see [RUNNING.md](./RUNNING.md).**

## How it works

1. **Parent uploads** a lesson PDF (e.g. `MATH3_Mod1.pdf` for addition or
   `MATH3_Mod2.pdf` for division from `backend/sample-modules/`, or any of
   their own PDFs).
2. The backend **extracts the text from the PDF** and sends it to a
   **generative AI provider** (OpenAI and/or Gemini, configurable via
   `AI_PROVIDER_ORDER`), which generates 3 short, visual, story-based
   mini-lessons — each with 5 multiple-choice questions — matched to the
   concept and difficulty found in the source material.
3. The parent gets a **join code** (Kahoot-style) for the child's device.
4. The **parent controls pace** (Next / Back + text-to-voice) and reads the
   **full story on one page**. The **student only sees PNG visuals** and can
   only tap Kahoot-style answer blocks.
5. After the session, the quiz results are sent back to the **generative AI**
   to **curate a feedback report** for the parent — highlighting strengths,
   error patterns, and multi-sensory practice recommendations tailored to
   what the child got right and wrong.

If no AI provider is configured or all configured providers fail, the app
falls back to a deterministic offline generator (and, for the sample PDFs
above, hand-authored demo packs in
[`backend/src/content/modules.config.js`](backend/src/content/modules.config.js))
so the product still works end-to-end without any external API key.

**Custom art:** overwrite PNGs in `frontend/src/assets/story/` keeping the same
filenames (`girl.png`, `apple.png`, `market.png`, …).

## Architecture

```
┌─────────────┐        REST/JSON        ┌──────────────┐        SQL        ┌───────┐
│  Frontend    │ ─────────────────────▶ │   Backend     │ ─────────────────▶│ MySQL │
│  React+Vite  │ ◀───────────────────── │  Express API  │ ◀──────────────── │       │
└─────────────┘                          └──────┬───────┘                    └───────┘
                                                 │
                                                 ▼
                                     ┌───────────────────────┐
                                     │ Generative AI provider  │
                                     │ (OpenAI / Gemini)        │
                                     │ story + feedback gen     │
                                     └───────────────────────┘
```

- **backend/** — Node.js + Express REST API, MySQL via Sequelize ORM. Handles
  auth, PDF text extraction, AI-generated story/quiz packs, join-code sessions
  with parent pacing cursor, quiz answers, and AI-curated feedback report
  generation.
- **frontend/** — React (Vite) single-page app for parent and student flows.
- **docker-compose.yml** — MySQL, backend, frontend, and Adminer.

## Key REST endpoints

| Method | Path | Description |
|---|---|---|
| POST | `/api/auth/register` | Create a parent account |
| POST | `/api/auth/login` | Log in |
| POST | `/api/modules/upload` | Upload a lesson PDF → generate stories via AI |
| GET | `/api/modules` | List a parent's modules |
| POST | `/api/sessions` | Generate a join code for a module |
| POST | `/api/sessions/join` | Student joins with a code |
| GET | `/api/sessions/:id/state` | Student polls parent cursor |
| POST | `/api/sessions/:id/advance` | Parent Next |
| POST | `/api/sessions/:id/back` | Parent Back |
| POST | `/api/quiz/answer` | Student submits one answer tap |
| GET | `/api/reports/session/:id` | Full parent feedback report |
| GET | `/api/reports/session/:id/summary` | Student-facing results summary |
| GET | `/api/reports/history` | Parent's history of completed modules |

## Tech stack

- **Frontend**: React 18, React Router, Vite, Axios — plain CSS, Web Speech API for parent text-to-speech.
- **Backend**: Node.js, Express, Sequelize, MySQL 8, JWT auth, Multer (uploads), OpenAI/Gemini SDKs for story and feedback generation.
- **Infra**: Docker, Docker Compose, nginx, Adminer.

## MVP notes

- Story visuals use **named PNG placeholders** in `frontend/src/assets/story/` (swap files to customize).
- Stories, questions, and feedback reports are **generated by AI** from the uploaded PDF's extracted text and the child's quiz results — configure `OPENAI_API_KEY` and/or `GEMINI_API_KEY` in `backend/.env` (see `backend/.env.example`).
- Parent owns pacing and TTS; student only taps answer choices.

## AI Disclosure
- OpenAI and Google SDK Integration for story generation and feedback
- Google Gemini for AI-assisted development
- Cursor for AI-assisted development
- Claude for Initial Project design
